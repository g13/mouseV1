      MODULE parameters
        IMPLICIT REAL*8(A-H,O-Z),INTEGER*4(I-N) 
        parameter ( nex = 72, ney = 120, nix = 36, niy = 60)
        parameter (nmax = nex * ney + nix * niy )
        parameter (nlx=16,nly=16,nlgn=nlx*nly)
        parameter (max_ncaj = 400)
      END MODULE
